{
  "name": "builder",
  "version": "1.0.0",
  "description": "React Architecture Builder monorepo",
  "private": true,
  "workspaces": [
    "login-app",
    "builder-app"
  ],
  "scripts": {
    "dev:example": "npm run dev --workspace=login-app",
    "dev:builder": "npm run dev --workspace=builder-app",
    "build:example": "npm run build --workspace=login-app",
    "build:builder": "npm run build --workspace=builder-app"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
